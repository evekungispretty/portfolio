{
  "buildCommand": "pnpm run build",
  "outputDirectory": "artifacts/portfolio/dist/public",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
